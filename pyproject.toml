[project]
name = "taller-libro"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "Django>=4.2,<5.0",
    "dj-database-url>=2.3",
    "psycopg[binary]>=3.2",
]

[tool.vercel.scripts]
build = "python manage.py migrate && python manage.py collectstatic --noinput"
